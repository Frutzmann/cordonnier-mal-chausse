# N8N Webhook base URL (used in components for tracking and form submission)
# N8N_WEBHOOK_URL=https://n8n.example.com

# No environment variables are required at build time.
# Webhook URLs are hardcoded in the Astro components (static site).
